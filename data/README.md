# data/

Datasets are not committed (this directory is in `.gitignore`; only this README is kept).

- **IEEE-CIS Fraud Detection**: `kaggle competitions download -c ieee-fraud-detection`
  (accept the competition rules on the website first).
- **Elliptic Data Set**: `kaggle datasets download ellipticco/elliptic-data-set`.

Phase 1 adds a downloader that prints the file sizes and waits for confirmation before downloading.
